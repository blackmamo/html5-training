import { SideValidatorDirective } from './side-validator.directive';

describe('SideValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SideValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
